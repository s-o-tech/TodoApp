<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <table>
        <thead>
            <tr>
                <th>target</th>
                <th>persent</th>
                <th>message</th>
            </tr>
        </thead>
        <tbody>
            
            <% for(let task of tasks){ %>
                <tr>
                    <td><%= task.target%></td>
                    <td><%= task.percent%>%</td>
                    <td><%= task.message%></td>
                </tr>
            <% }%>
        </tbody>
    </table>


  </body>
</html>
